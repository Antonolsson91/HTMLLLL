function reverseDirection(el) {
    var elem = document.getElementById(el);
    elem.style.animationDirection = "reverse";
}
function normalDirection(el) {
    var elem = document.getElementById(el);
    elem.style.animationDirection = "normal";
}